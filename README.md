# inventory-management-system-sqlite
An inventory management system built using SQLite with structured relational database design. The project demonstrates table relationships (Users, Categories, Items), foreign key constraints, and core database operations including CRUD functionality, seed data initialization, and basic authentication handling.
